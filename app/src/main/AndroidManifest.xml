<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.techneapps.notestaking">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeDark"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".view.settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".view.splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.viewnote.allnotes.AllNotesViewerActivity"
            android:label="@string/app_name"
            android:theme="@style/AppThemeDark.NoActionBar" />
        <activity android:name=".view.addnote.AddNewNoteActivity" />
        <activity android:name=".view.viewnote.singlenote.SingleNoteViewerActivity" />
        <activity android:name=".view.editnote.EditNoteActivity" />
    </application>

</manifest>